# FLRTS Extensions - Python Dependencies
# Frappe and ERPNext are provided by the Frappe Cloud platform

# Testing dependencies (development only)
pytest>=7.0.0
pytest-cov>=4.0.0

# HTTP requests for external API calls
requests>=2.31.0

# Redis Queue for background jobs (provided by Frappe)
rq>=1.15.0
